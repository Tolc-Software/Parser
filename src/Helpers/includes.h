#pragma once

#include <string>
#include <vector>

namespace Helpers {

// Return a vector of include paths to the standard library used
std::vector<std::string> getSystemIncludes();
}    // namespace Helpers
